# PADjs

Open `index.html` in Safari.

Blocks lift. Sockets pull a wire. Hover or tap a mark for the line of context. The lock waits on a predecessor. The box is the fire safe: papers in, printer next.
